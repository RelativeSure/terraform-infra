# terraform-infra

## Pre-commit
General hygiene hooks are provided via `pre-commit` (Terraform linting is handled by MegaLinter).

Setup:
```bash
pip install pre-commit
pre-commit install
pre-commit run --all-files
```
